/**
 * Entity Change Type Enum
 * Translated from @volo/abp.ng.audit-logging v2.7.0
 */

/**
 * Enum for entity change types.
 * Represents the type of change made to an entity.
 * @since 2.7.0
 */
export enum eEntityChangeType {
  Created = 0,
  Updated = 1,
  Deleted = 2,
}
