/**
 * Audit Logging Component Identifiers
 * Translated from @volo/abp.ng.audit-logging v2.4.0
 */

/**
 * Enum for audit logging component identifiers.
 * Used for component registration and identification.
 * @since 2.4.0
 */
export enum eAuditLoggingComponents {
  AuditLogs = 'AuditLogging.AuditLogsComponent',
}
