/**
 * SaaS Enums
 * @since 2.4.0
 * @updated 2.7.0 - Added route-names export
 * @updated 3.0.0 - route-names moved to config, re-exported here for backwards compatibility
 */
export * from './components';

// Route names are now in config, but re-export for backwards compatibility
// Note: Administration key has been removed in v3.0.0
export * from './route-names';
