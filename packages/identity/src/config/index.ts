/**
 * Identity module configuration exports.
 *
 * @since 3.0.0
 *
 * This subpackage provides:
 * - eIdentityPolicyNames: Policy names for authorization checks
 * - eIdentityRouteNames: Route names for navigation (without Administration key)
 * - IDENTITY_ROUTE_PROVIDERS: Route configuration provider
 * - configureRoutes: Function to configure identity routes
 * - initializeIdentityRoutes: Helper to initialize routes with global RoutesService
 */

export * from './enums';
export * from './providers';
